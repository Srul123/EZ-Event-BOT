{
  "name": "ez-event-bot",
  "version": "1.0.0",
  "private": true,
  "description": "EZ-Event BOT monorepo",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@ez-event-bot/bot-service",
    "build": "npm run build -ws --if-present"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
